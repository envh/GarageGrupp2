@model IEnumerable<Garage2._0.Models.ParkedVehicle>

@{
    ViewBag.Title = "Index";
}

<h2>Parked in Joe's Garage</h2>

<p>
    @Html.ActionLink("Park a new vehicle", "Create", new { searchProp = ViewBag.SearchProp, searchValue = ViewBag.SearchValue })
</p>
<select name="searchProp" form="searchform" id="searchProp" onchange="AdaptForm()">
    <option value="RegNo">Reg No</option>
    <option value="Type">Type</option>
    <option value="Colour">Colour</option>
    <option value="CheckInTime">Check In Time</option>
</select>
<form id="searchform">
    <input name="searchValue" id="select" type="text" />
    <input type="submit">
</form>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Colour)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Brand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AmountOfWheels)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheckInTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RegNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Colour)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Brand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Model)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AmountOfWheels)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheckInTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Check out from Garage", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<div id="backLink">
    @{
        if (ViewBag.InsertLink)
        {
            @Html.ActionLink("Back to full list", "Index")
        }
    }
</div>

<script>
    AdaptForm = function ()
    {
        form = document.getElementById("searchform")
        form.removeChild(document.getElementById("select"))
        if (document.getElementById("searchProp").value == "Type")
        {
            var array = ["Car", "Bus", "Airplane", "Motorcycle", "Boat"]
            var select = document.createElement("select");
            select.name = "searchValue";
            select.id = "select";
            form.insertBefore(select, form.firstChild);

            for (var i = 0; i < array.length; i++)
            {
                var option = document.createElement("option");
                option.value = array[i];
                option.text = array[i];
                select.appendChild(option);
            }
        }
        else
        {
            var text = document.createElement("input");
            text.setAttribute("type", "text");
            text.setAttribute("name", "searchValue")
            text.id = "select";
            form.insertBefore(text, form.firstChild);
        }
    }

    //AddBackLink = function()
    //{
    //    var pos = document.getElementById("backLink");
    //    var link = document.createElement("a");
    //    link.setAttribute("href", "/");
    //    link.innerHTML = "Back to full list";
    //    pos.appendChild(link);
    //}
</script>